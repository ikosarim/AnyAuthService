apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: any-authenticate-service-ingress
spec:
  rules:
    - host: "any-authenticate-service.k8saas.timeweb.com"
      http:
        paths:
          - pathType: Prefix
            path: "/any-authenticate-service"
            backend:
              service:
                name: any-authenticate-service
                port:
                  number: 8080